// check if user is logged in
let loggedIn = false; // replace with your logic

// show overlay if user is not logged in
if (!loggedIn) {
  document.getElementById("overlay").classList.add("visible");
}